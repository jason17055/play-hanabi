Create a 'extlib' subdirectory, and copy into it:
 * jackson-core-2.1.0.jar
 * tomcat-servlet-api.jar

For the `ant deploy' target, create a tomcat_libs subdirectory and
copy into it:
 * all the jars from the Tomcat 'lib' directory
 * tomcat-juli.jar from the Tomcat 'bin' directory
